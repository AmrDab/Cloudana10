VITE_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id
# Edge API (Cloudflare Worker): templates, pouw, payments, faucet, hardware-scan, auth
VITE_API_URL=http://localhost:7002
# Node orchestrator (verify, build-provider, deploy, orchestration, provider-logs,
# workload-status). Set to the deployed orchestrator URL (e.g. Akash). Falls back to
# VITE_API_URL when unset.
VITE_NODE_API_URL=http://localhost:7002
WS_RPC_URL=wss://base-sepolia.g.alchemy.com/v2/your_alchemy_key
VITE_PINATA_JWT=your_pinata_jwt_token
VITE_PINATA_GATEWAY=your-gateway.mypinata.cloud
PORT=7003
