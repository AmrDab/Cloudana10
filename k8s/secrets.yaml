apiVersion: v1
kind: Secret
metadata:
  name: cloudana-secrets
  namespace: cloudana
type: Opaque
stringData:
  # Replace these with real values before applying
  ORCHESTRATOR_PRIVATE_KEY: "replace-me"
  JWT_SECRET: "replace-me-min-32-chars"
  MONGODB_URI: "mongodb://mongodb:27017/cloudana"
  ORCHESTRATOR_CHAIN_WSS_URL: "wss://base-sepolia.g.alchemy.com/v2/replace-me"
